<!DOCTYPE html>
<html>
  <head>
  <!-- title method is defind in app/helpers/application_helper-->
    <title><%= yield :title %>Tsil Odot</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <%= render partial: "layouts/flash" %>

    <header class = "row global-header">
    <!-- screen sizes -->
      <div class = "small-12 small-centered medium-8 medium-centered large-5 large-centered columns">
        <% if logged_in? %>
          <%= render partial: 'layouts/header_logged_in' %>
        <% else %>
          <%= render partial: 'layouts/header_logged_out' %>
        <% end %>
      </div>

    </header><!-- /header -->

    <!-- row collapse create a new row, will make sure  collumns expand edge to edge when it gets to smaill-12-->
      <div class = "row collapse">
          <div class = "small-12 small-centered medium-8 medium-centered large-5 large-centered columns">
            <%= yield %>
          </div>
      </div>

    <footer class = "row global-footer">
      <div class = "small-12 small-centered medium-8 medium-centered large-5 large-centered columns text-center">
        <% if logged_in? %>
          <%= link_to "Settings", edit_user_path(current_user) %>
        <% end %>
      </div>
    </footer>

  </body>
</html>
